# Build de produção: o frontend é servido pelo próprio backend (mesmo host/porta),
# então a API é relativa — funciona em qualquer IP da rede sem rebuild.
VITE_API_BASE_URL=/api/
VITE_ENVIRONMENT=production
